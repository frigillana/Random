<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectdatapassing">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectDataPassing">
        <activity android:name=".challenge2.Challenge2SecondActivity" />
        <activity android:name=".challenge2.Challenge2FirstActivity" />
        <activity android:name=".challenge1.Challenge1SecondActivity" />
        <activity android:name=".challenge1.Challenge1FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>